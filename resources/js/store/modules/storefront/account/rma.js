// initial state
const state = {}

// getters
const getters = {}
  
// actions
const actions = {

    async rmaRequest({}, formdata) {
        alert('Request sent!')
    },
}

// mutations is often used to filter results
const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}